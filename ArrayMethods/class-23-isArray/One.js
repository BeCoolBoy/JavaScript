// Array.isArray() it is used for to find the given variable is array or not

const message = ['hi','hoi','hello'];
const age = 23;
const name = 'karthik';

console.log(Array.isArray(message))
console.log(Array.isArray(age))
console.log(Array.isArray(name))