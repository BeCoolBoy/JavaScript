// template literal will work within the ( back tick );
// template literal it won't work inside the double and single quites;

const a = 10;
const b= 5;
const c= 15;
console.log(`Result is ${a+b+c}`); 